Human

At:
  workflow_dispatch:
    inputs:
      agent_name:
        description:
        required: true
        default: 'ASEP9929'
      agent_description:
        description: 'ASEP9929'
        required: true
        default: 'ASEP9929 AI Agent'

Jobs:
  checking:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Step 1 - Install ClawHub skills
        run: npx clawhub@latest install verified-agent-identity

      - name: Install script dependencies
        run: |
          cd scripts
          npm install

      - name: Step 2 - Create a new Ethereum Identity
        run: node scripts/createNewEthereumIdentity.js

      - name: Step 3 - Connecting Human to Agent
        run: |
          node scripts/manualLinkHumanToAgent.js --challenge '{"na 
